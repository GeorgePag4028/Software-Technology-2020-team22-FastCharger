code . (anoigw to folder)

git status
git add . 
git commit -m "description"
git push origin tempLefteris (push sto branch mou)


git branch ( blepw se poio branch eimai kai ti branch uparxoun)
git checkout branch_name ( gia na alla3w branch)

